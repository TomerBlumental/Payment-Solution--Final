/**
 * @NApiVersion 2.x
 * @NScriptType bankConnectivityPlugin
 */
define(['N/crypto', 'N/crypto/certificate', 'N/file', 'N/https', 'N/https/clientCertificate', 'N/plugin', 'N/query', 'N/record', 'N/sftp'],
/**
 * @param{crypto} crypto
 * @param{certificate} certificate
 * @param{file} file
 * @param{https} https
 * @param{clientCertificate} clientCertificate
 * @param{plugin} plugin
 * @param{query} query
 * @param{record} record
 * @param{sftp} sftp
 */
function(crypto, certificate, file, https, clientCertificate, plugin, query, record, sftp) {
   
    return {
        
    };
    
});
